import React from 'react';

const CardAdd = () => {
    const {
      token: { colorBgContainer, borderRadiusLG },
    } = theme.useToken();
  
    return (
        <div>
            On create
        </div>
    );
  };

export default CardAdd;